# Production environment
# This URL is used when the app is deployed (e.g. Netlify).
# Requests to this URL will fail immediately (no backend deployed yet),
# which triggers the offline mock-data fallback built into every service.
#
# When you deploy the backend (Render, Railway, etc.), replace this with the real URL:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=https://api.luxacademy.invalid
